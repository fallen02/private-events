<nav class="bg-white border-gray-200 dark:bg-gray-900">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
      <% if user_signed_in? %>
        <h1 class="font-bold text-3xl">Welcome, <em class="text-red-900"><%= current_user.name %></em></h1>
      <% else %>
        <h1 class="font-bold text-3xl">Welcome, <em class="text-red-900">guest</em></h1>
      <% end %>
    </a>
    <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse">
      <% if user_signed_in? %>
        <%= render 'shared/profile'%>
      <% else %>
        <%= button_to "Sign in", new_user_session_url, method: :get, class: "text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800" %>
      <%end %>
    </div>
  </div>
</nav>